import { TipoContaPage } from "@/components/Pages/TipoContas";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Tipo de Contas",
};

const TipoDeContas = () => <TipoContaPage />;

export default TipoDeContas;
