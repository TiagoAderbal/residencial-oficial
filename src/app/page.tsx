import { HomePage } from "@/components/Pages/Home";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Home page",
};

const Home = () => <HomePage />;

export default Home;
