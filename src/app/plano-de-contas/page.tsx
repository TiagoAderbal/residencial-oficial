import { PlanoDeContasPage } from "@/components/Pages/PlanoContas";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Plano de Contas",
};

const PlanoDeContas = () => <PlanoDeContasPage />;

export default PlanoDeContas;
